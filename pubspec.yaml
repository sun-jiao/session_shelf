name: session_shelf
description: The `session_shelf` is the implementation of `cookiesMiddleware` and  `sessionMiddleware` for `shelf`, to store sessions in files or SQL databases as plain or encrypted text.
repository:  https://github.com/sun-jiao/session_shelf
version: 0.2.0
environment:
  sdk: '>=2.18.0 <4.0.0'
dependencies:
  cryptography: ^2.7.0
  #  dart_frog:
  #    git:
  #      url: https://github.com/sun-jiao/dart_frog.git
  #      ref: fix773
  #      path: packages/dart_frog
  path: ^1.9.0
  shelf: ^1.4.0
dev_dependencies:
  lints: ^2.0.0
