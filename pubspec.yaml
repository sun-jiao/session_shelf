name: shelf_sessions
description: The `shelf_sessions` is the implementation of `cookiesMiddleware` and  `sessionMiddleware` for `shelf`.
repository:  https://github.com/sun-jiao/shelf_sessions
version: 0.1.2
environment:
  sdk: '>=2.18.0 <3.0.0'
dependencies:
  cryptography: ^2.7.0
  path: ^1.9.0
  shelf: ^1.4.0
dev_dependencies:
  lints: ^2.0.0
